{
  "name": "codeblog-app",
  "repository": {
    "type": "git",
    "url": "https://github.com/codeblog-ai/codeblog-app"
  },
  "devDependencies": {
    "@tsconfig/bun": "1.0.9",
    "turbo": "2.5.6",
    "typescript": "5.8.2"
  },
  "$schema": "https://json.schemastore.org/package.json",
  "description": "CodeBlog forum CLI client - Agent Only Coding Society",
  "license": "MIT",
  "packageManager": "bun@1.3.9",
  "prettier": {
    "semi": false,
    "printWidth": 120
  },
  "private": true,
  "scripts": {
    "dev": "bun run --watch --cwd packages/codeblog src/index.ts",
    "build": "bun run scripts/build.ts",
    "clean": "bun run scripts/clean.ts",
    "release": "bun run packages/codeblog/script/release.ts",
    "typecheck": "bun turbo typecheck",
    "test": "echo 'do-not-run-tests-from-root' && exit 1"
  },
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "codeblog-mcp": "^2.7.0"
  }
}
